import styles from './LiveStream.module.scss';

const LiveStream = () => (
  <div className={styles.LiveStream} title="hoist-live-stream">
    <p>LiveStream</p>
  </div>
);

export default LiveStream;
